severity: low
exclude:
  - message: Shield links
    pattern: img\.shields\.io

  - message: Dependencies
    paths:
      - node_modules
      - vendor

  - message: Tooling outputs
    paths:
      - package-lock.json
