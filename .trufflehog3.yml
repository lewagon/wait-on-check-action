severity: low
exclude:
  - message: Shield links
    pattern: img\.shields\.io

  - message: Workflow action SHAs
    pattern: uses:\s.+@.+

  - message: Dependencies
    paths:
      - node_modules
      - vendor

  - message: Tooling outputs
    paths:
      - .yardoc
      - doc
      - package-lock.json
